import React from "react";
import { View, Text } from "react-native";

export default function Nataly() {
    return(
        <View>
            <Text>Nataly Baratella</Text>
            <Text>Pimeira prova DDM lado B</Text>
        </View>

    );
}

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });